---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: seichireviewgateways.seichiclick.seichi.click
spec:
  group: seichiclick.seichi.click
  names:
    kind: SeichiReviewGateway
    listKind: SeichiReviewGatewayList
    plural: seichireviewgateways
    singular: seichireviewgateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SeichiReviewGateway is the Schema for the seichireviewgateways
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SeichiReviewGatewaySpec defines the desired state of SeichiReviewGateway
            properties:
              pullRequestNo:
                description: 稼働環境を生成する時にどのプルリクエスト番号時点のリポジトリデータを参照するかの設定
                type: integer
            required:
            - pullRequestNo
            type: object
          status:
            description: SeichiReviewGatewayStatus defines the observed state of SeichiReviewGateway
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
